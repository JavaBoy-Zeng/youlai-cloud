请在当前 Spring Boot 项目中实现一个决策引擎 MVP。

目标：
1. 新增规则表、规则条件表、规则动作表
2. 支持 IF 条件判断
3. 支持 AND / OR 条件组合
4. 提供 /decision/execute 接口
5. 返回命中规则、决策结果、traceId
6. 先不要做前端，只做后端

约束：
- 使用 MyBatis-Plus
- 使用现有项目包结构
- 不要改动已有鉴权逻辑
- 每次修改前先说明会改哪些文件
- 改完后给我启动和测试命令



第一步：让它只分析项目结构，给方案，不改代码
第二步：你确认方案
第三步：让它只实现一个小闭环
第四步：跑通后再扩展


先不要修改代码。请先阅读项目结构，告诉我实现这个功能需要改哪些文件、每个文件改什么、可能的风险点。


你是我的 Java 后端开发助手。当前项目是 Spring Boot / Spring Cloud / MyBatis-Plus 项目。

任务目标：
【这里写你要做的功能】

要求：
1. 先阅读相关文件，不要马上修改代码
2. 先输出实现方案和涉及文件
3. 确认后再开始改代码
4. 每次只完成一个小闭环
5. 不要全项目乱改
6. 不要改无关代码
7. 修改后给出启动命令、测试接口、回滚点
8. 如果连续两次报错，不要继续盲修，先总结原因